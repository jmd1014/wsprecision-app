# 우성정밀 업무관리 시스템 (wsprecision-app)

부산 우성정밀의 수주·발주·재고·생산·출고·원가를 관리하는 사내 웹앱.
**실사용자 6명이 매일 쓰는 운영 시스템**이다 — main 푸시 = 즉시 배포임을 항상 의식할 것.

## 아키텍처

- 앱: Streamlit 단일 파일 `streamlit_app.py` (~1만 줄) + `utils/`(문서 생성·파서) + `db.py`(Supabase REST 래퍼)
- DB: Supabase PostgreSQL (프로젝트 `iryhamvxpboumnrtajwb`, 서울 리전)
- 배포: Streamlit Community Cloud — **main 브랜치 푸시 시 자동 배포** (https://wsprecision.streamlit.app)
- DB 접속: **service_role 키만 사용** (RLS 우회). anon 키 경로는 전부 RLS로 차단되어 있음
- 인증: 자체 로그인 (사용자·비밀번호 해시는 DB `app_settings`에 저장)

## 이 PC 개발 환경 (주 개발 머신)

- 저장소: `C:\Users\우성 02\wsprecision-app` — **작업은 junction `C:\wsapp` 경로로** (한글 경로가 cmd/도구에서 인코딩 문제를 일으킴)
- Python: `C:\Python311\python.exe` (3.11.9, NuGet zip 설치 — 이 PC는 MSI 설치가 실패함)
- 실행: `C:\Python311\python.exe -m streamlit run streamlit_app.py` (로컬 미리보기는 `.claude/launch.json`의 "wsprecision" 구성, port 8501)
- 테스트: `C:\Python311\python.exe -m pytest tests -q` (AppTest 기반 — st.dataframe→toss_table 전환 시 관련 테스트 확인)
- 로컬 secrets: `.streamlit/secrets.toml` (커밋 금지, `auth.disabled=true`로 로그인 우회) — 키 원본은 Supabase 대시보드·Streamlit Cloud Secrets

## 작업 흐름

1. `design/...` 또는 `feat/...` 브랜치에서 작업
2. 로컬 미리보기로 화면·기능 검증 (9개 페이지 예외 없이 렌더되는지)
3. 사용자 확인 후 main 머지 → 자동 배포
4. 커밋 메시지: `feat:`/`fix:`/`design:` + 한글 요약 + 본문에 변경 항목 나열

## 디자인 시스템 (토스 스타일, 2026-08-12 확정)

- 폰트 **Pretendard** / 주색 **#3182f6** (hover #1b64da) / 배경 #f9fafb
- 카드: 테두리 없음 + radius 16px + `var(--shadow)` / 버튼: radius 10px, 보조는 회색 면 #f2f4f6
- 상태 색 규칙(전 페이지 고정): 문제=#f04452 / 대기·부분=#dd6b02 / 완료=#01a76b / 진행·활성=#3182f6
- **이모지로 상태 표시 금지** — 색·배지로만 (신호등 이모지는 2026-08-12 제거됨)
- 토큰·컴포넌트 CSS는 `streamlit_app.py` 상단 CSS 블록 한 곳에만 정의
- 읽기 전용 표에서 배지·강조가 필요하면 `toss_table()` 헬퍼 사용 (st.dataframe은 CSS 불가). 편집 표는 data_editor 유지
- 인쇄물(거래명세서·라벨·영업보고 HTML)은 **인쇄용 자체 디자인 유지** — 앱 토큰 적용 대상 아님

## 공정 라우팅·배치 (2026-08-12 확정)

- **BOM = 정보의 진실(소재·공정·수량·원가), 라우팅 = 그 행들에 순서 부여** (2026-08-20 확정,
  BOM 이 상위 개념). 마스터 관리 > BOM 편집 탭 하위: [BOM](행 추가·일괄 편집·자재 교체) +
  [라우팅 (공정 순서)](스텝 = 기본 공정 4종 + 그 제품 BOM 공정행 선택, 자유 텍스트·자동
  원가행 생성 없음). 라우팅 편집기에 단가 표시 금지. `default_vendor` = PPAP 승인 업체
  고정(라우팅에서만 변경). 라우팅 있는 제품의 HEAT/SURFACE/OUTSOURCE 원가행이 순서
  미부여면 정합 점검 `ROUTE_COST_ORPHAN`(Migration 044)에 표기
- **BOM 의 기준 단위는 품번** (2026-08-20 사용자 확정): 제품을 골라 그 제품의 BOM 이
  전개되는 흐름 — 항목(bom_id) 단독 취급·노출 금지 (같은 이름 구분 등 불가피할 때만).
  자재행/공정행 추가·자재 교체는 선택된 제품 스코프에서. bom_id 는 내부 키로만
- **BOM 편집 탭 = 한 화면** (2026-08-20 확정, 하위 탭·일괄 편집 표 없음): 제품 리스트
  (toss_grid, BOM 소재·공정 수·라우팅 여부 요약) → BOM 행 리스트 → 상세 카드 —
  소재행 카드(자재 검색 매칭 + 수량/분할/검증) / 공정행 카드(공정명·종류·LOT·업체).
  공정 업체는 계열별 거래처만(HEAT→HEAT_TREAT, SURFACE→SURFACE, OUTSOURCE→OUTSOURCE,
  `bom.process_vendor_id`). 행 삭제는 confirm_gate + 참조 라우팅 스텝 동시 제거
- **라우팅 편집 = 드래그 전용** (2026-08-21 확정, 구성 선택 없음): 공정 풀 = 생산·검사
  + 그 제품 BOM 공정행 전부 자동 포함(빼려면 BOM 행 삭제), **소재입고(시작)·완성(끝)은
  고정 칩**(회색 점선 — 드래그 영역 밖). 가운데만 streamlit-sortables 로 드래그
  (custom_style 토스 칩, TypeError 폴백 포함, 미탑재 시 기존 순서). 번호 플로우
  미리보기(고정=회색 점선, BOM 공정=파랑+업체명/미매칭). 외주 스텝 default_vendor 는
  BOM 공정행 매칭 업체를 저장 시 복사 (업체 변경 후 라우팅 재저장 필요)
- **공정 처리 스테퍼는 배치 위치 기반** (2026-08-21): 열린 배치가 있는 지시는 배치가
  있는 스텝 = 진행(on), 그 앞 = 완료(done) — 수량 추정 로직은 배치 없는 옛 지시 전용.
  외주 출고의 의뢰서 즉시 다운로드 박스는 제거됨 — 출력은 처리 이력 > 라벨·의뢰서
  재발행에서 (이동표와 중복 제거)
- 납품 스케줄 뷰 = 회차 간트 + 납기 입력 2종 (주차별 물량 뷰는 2026-08-21 제거)
- **공정행 표준 단가는 BOM 공정행 카드에서 직접 입력** (2026-08-21 사용자 확정 —
  단가 관리와 같은 unit_price). **공정 이동표** = 배치 카드 [이동표 발행]
  (`batch_labels()`, 배치번호·품번·수량·공정·위치·작업지시·소재 LOT) — 공정이 바뀔
  때마다 재발행해 실물에 부착
- **편집 표 UI 착시 주의**: data_editor 의 편집 상태는 저장 전에도 세션에 남아 화면상
  반영된 것처럼 보인다(DB 는 안 바뀜) — 편집 표에는 [편집 취소] 버튼(위젯 key nonce
  리셋)을 두고, 저장 성공 시 적용 내역을 자동 표시 + nonce 리셋할 것
- **투입 전 '소재 단계' 외주 폐지** (2026-08-20 사용자 확정, Migration 045): 투입 등록은
  투입만 하고, 생산 전 외주(고용화 등)를 포함한 모든 공정은 투입 후 라우팅 순서대로 배치
  단위 처리. 초기 배치는 라우팅의 첫 공정에서 시작. `wo_stage_qty` 는 생산 전 외주
  미회수분(pre_out)을 생산중에서 제외
- 기본 플로우: 소재입고 → 생산 → 검사 → 완성 (외주 없음). 외주 있는 제품만 라우팅 정의
- 순차 강제: 라우팅 제품은 스텝별 출고/회수 누계 기준 — 인수 전량 통과 전 검사 잠금
- **배치 상태 기계 (2026-08-27 확정, 용어 2026-08-28 확정)**: 배치 위치 = 공정 +
  `step_status`(WAIT 시작 대기/RUN 진행 중, Migration 049). **용어: '투입'은 소재
  투입(투입 등록·투입 취소)에만, 공정 단위는 '시작/완료'** — [공정 시작](대기→진행)·
  [공정 완료](→다음 공정 시작 대기), 사내 공정은 [시작+완료 한번에] 보조. 외주는
  출고=시작·입고=완료, 생산(MES)은 시작 후 [완료 등록], 검사는 시작 후 판정([전량 합격]
  퀵 + 상세)이 완료. **[공정 취소] = 배치의 마지막 이벤트가 그 공정 시작일 때만**(이후
  처리·분기 시 차단 — 추적성 보호), 역이벤트 STEP_CANCEL. 스테퍼: 소재입고 칸 폐지,
  지나간 공정=초록·진행=파랑·시작 대기=주황 테두리(.step.wait). 합치기는 같은
  공정·위치·상태끼리만. 라우팅 데이터의 MAT_IN 은 호환용 자동 삽입(표시 안 함).
  **투입 등록의 작업지시 NO 는 필수**(현장·MES 연결 키 — 미입력 시 버튼 잠금)
- **선택 리스트 상세 입력은 `fresh_keys()` 필수** (2026-08-27): 선택 id·기본값 지문이
  바뀌면 입력 키 리셋 — 이전 선택의 값 잔존으로 인한 오입력 방지. 새 상세 영역을 만들면
  같은 패턴 적용
- **배치(wo_batches)**: 공정을 흐르는 수량의 정체성. 배치번호 = `지시번호-가지`(예: 20260812-003-A),
  분기·합류 계보는 `batch_links`(SPLIT/MERGE). **합류는 같은 작업지시 안에서만.**
  **`_bat_take` 분기는 부모 위치(공정·상태·장소)를 승계**하고 new_fields 만 덮어쓴다 —
  호출부가 위치 필드를 생략해도 안전 (2026-08-28 검사 부분 시작 → 생산 분기 사고 수정).
  Phase B·C 적용됨 — 공정 처리는 배치 단위(행 선택 → 위치가 액션 결정, 부분 수량=자동 분기,
  완성 LOT 번호=배치번호), 공정 관리 > LOT 추적 탭에서 소재→공정→완성→출고 계보 조회.
  배치 없는 옛 지시만 수량 풀 방식 유지. 배치 처리 영역은 파란 헤더 밴드+카드로 구분(.bt-hdr)
- 선택 UI 패턴: **선택이 필요한 리스트는 `toss_grid()`** (토스 CSS AgGrid — 행 클릭=선택,
  인덱스 반환, 미선택·미탑재 시 첫 행/st.dataframe 폴백 — AppTest 호환).
  보기 전용 표는 `toss_table()` — 두 컴포넌트가 같은 시각 언어(배지·정렬·카드)를 공유

## 데이터 정합 원칙 (2026-08-19 평가로 확정 — "진실은 한 곳")

- **소재 정보의 진실 = BOM(material_id)**. 제품 마스터의 소재 텍스트(raw_material_name 등)는
  표시용이며 BOM 변경 시 자동 동기화된다(편집 잠금). 원가·투입·발주 추천은 BOM만 본다
- **수주 진행 대사 불변식**: 회차 delivered 합 = 라인 received_qty − presched_qty(스케줄 이전
  기납품, Migration 038). 출고 확정은 등록 때 지정한 회차를 우선 충당
- **품번의 진실 = product_id** (pn은 사람용 표기 — 저장 시 공백 자동 제거). 텍스트 조인 지양
- **발주 잔량 종결은 라인(제품) 단위** — `purchase_order_items.closed_at`(Migration 043).
  뷰 `po_item_receipt_v`가 라인 종결·발주 CLOSED 모두 pending 0 처리. 전 라인 완료 시
  발주 헤더 자동 승격(RECEIVED/CLOSED), 해제는 발주 이력 > 종결 해제
- **발주서 작성 = 폼 3개 + PDF** (2026-09-09 사용자 피드백: 저장·로딩이 잦고 불편,
  완성은 PDF): ① 거래처(그룹 라디오 + selectbox, 신규 등록은 st.form) ② 품목 담기 —
  검색 폼([검색] 버튼, 검색어 없이 누르면 이 거래처 이력 전체) → 결과 표에서 체크해
  [체크한 품목 담기] 한 번(거래처 최근 단가·수량·최근 발주번호 프리필 — 품명이
  '품번 (사이즈)' 여도 품번으로 찾음). 결과 표의 '소재 (BOM 자재명)' = 발주서 규격,
  재질 = 제품 마스터 재질. 최근 발주 복사·품번 일괄 추가는 제거(2026-09-11: 발주서
  단위라 품목이 안 보이고 일괄은 오매칭) — 마스터에 없는 자재 직접 입력만 expander
  (발주서 라인에만 기재, 마스터 미등록) ③ 발주서 = st.form 하나(품목 data_editor
  편집 + '제외' 체크 + 발주 정보 + [발주서 만들기 (PDF)] / [편집 반영 (임시)] /
  [표 비우기]) — 폼 안에서는 rerun 없음. 편집 반영은 세션 임시 상태(DB 저장 아님). 생성 결과는 `po_result` 세션에 두고 rerun 뒤 PDF(기본)·xlsx 다운로드 +
  [새 발주서 시작]. PDF 는 utils/po_pdf.py(fpdf2 + 저장소 fonts/NanumGothic OFL 임베드,
  A4 1장, 품목 8행 미만은 빈 줄 채움, 바닥글은 auto_page_break 끄고 고정). 발주 이력
  상세는 PDF·xlsx 재발급 download_button 을 바로 노출(클릭 후 생성 아님)
- **발주 상태는 자동으로만 변한다** (2026-08-20 사용자 확정) — 입고=PARTIAL/RECEIVED,
  라인 종결=CLOSED 승격, 취소=[발주 취소] 버튼(입고 기록 있으면 차단). 상태 수기 변경 UI 금지
- **되돌리기 어려운 액션은 `confirm_gate()` 2단계 확인** (2026-08-20 사용자 확정) —
  잔량 종결·발주 취소·입고 취소·자재 병합·확정 전표 취소·라우팅 삭제·계정 삭제 적용됨.
  새 파괴적 버튼을 만들면 같은 패턴을 쓸 것 (사유 텍스트 강제 대신 확인 게이트)
- **마스터 편집 공통 문법** (2026-08-20): 거래처·제품·자재 편집은 toss_grid 행 선택 →
  상세 편집 카드(단건 저장·휴면 처리)가 기본, 대량 수정용 data_editor 는
  "일괄 편집 (표)" expander 로 보조
- **상세 편집 카드·일괄 편집 표·라우팅 편집기는 `st.form` 으로 감싼다** (2026-08-20
  사용자 피드백: 입력/셀 수정마다 rerun 로딩 방지 — 저장 제출 때 한 번만 실행).
  단, 검색→후보 표시처럼 입력에 반응해야 하는 흐름은 form 밖에 둔다
- **toss_grid 리스트: 12행 이하 전체 펼침, 13행 이상 고정 높이(520) + 내부 스크롤**
  (2026-09-03 사용자 확정 — 전체 펼침은 페이지가 길어 불편). 내부 스크롤이 죽던 원인과
  대책은 유지할 것: ① 탭 안에서 숨겨진 채 마운트되면 iframe 300px·컨테이너 폭 0 으로
  굳음 → 전역 CSS `iframe[title=st_aggrid…]{width:100%}` + custom_css `#gridContainer
  {width:100%}` ② 스크롤바 폭 0 측정·캐시 → gridOptions `scrollbarWidth=10` +
  `alwaysShowVerticalScroll` ③ 행 가상화가 iframe 안에서 첫 화면만 그림 →
  `suppressRowVirtualisation`. 검증은 AppTest 로 안 되므로(AgGrid 는 iframe) 운영과 같은
  Streamlit 버전의 브라우저에서 휠·드래그·행 클릭까지 확인할 것 (자동화 도구의 첫 휠
  이벤트는 iframe 에 안 들어가니 두 번째 휠로 판단)
- **자동 로그인(로그인 유지) = 브라우저 컴포넌트가 쿠키를 읽는다** (2026-09-03 재설계,
  실제 크롬으로 운영 검증). Streamlit Cloud 프록시는 **쿠키 헤더를 앱 서버에 전달하지
  않아 `st.context.cookies` 가 운영에서 항상 비어 있다** — 서버 헤더 기반 자동 로그인은
  로컬에서만 된다. 읽기는 `extra_streamlit_components.CookieManager(key="ws_cm").get_all()`
  (첫 런 None → 마운트 후 값과 함께 rerun, 그래서 로그인 폼을 같이 그려 둔다),
  쓰기·삭제는 components.html JS(매 런 기록, 로그아웃 시 삭제) 유지. 토큰은 HMAC
  `user|exp|sig` 14일 sliding. `auth_skip_cookie` 는 로그아웃 직후 재로그인 루프 방지
- **정합 감시 = `data_quality_v`** → 마스터 관리 > 정합 점검 탭. 전 항목 0건 유지가 목표.
  새 규칙을 만들면 검사도 이 뷰에 추가할 것
- **원가 로드맵 (2026-08-21 사용자 확정)**: ① 표준 단가 = BOM 공정행 unit_price
  (원가 확인 > 단가 관리에서 입력, product_cost_full_v 가 계산) — 지금 기준 ②
  BOM·공정 관리 완성 우선 ③ 외주·매입 실적이 쌓이면 LOT 별 실적 평가(표준 vs 실적)를
  추가. 완성도 감시: 정합 점검 PROC_NO_PRICE(단가 미입력)·PROC_NO_VENDOR(업체 미매칭,
  Migration 048 — 라우팅에 쓰이는 공정행 한정)
- **소재비 = purchase_ledger 매칭 기반** (2026-08-31 활성화): 소재행 단가는
  `purchase_ledger.matched_material_id` → `material_price_v.price_last` → legacy
  `products.material_unit_price` 순 fallback (product_bom_cost_v). BOM 소재행
  unit_price 에 소재 단가를 직접 넣지 않는다. 매칭 규칙(utils/purchase_sync.py):
  재질+치수+형상(환봉/육각) 완전 일치 · 후보 1개 · EA 단위 · 단가>0 만 자동 —
  KG 단위 매입은 환산 로직 도입 전까지 매칭 보류(행만 추가). 회귀 테스트
  tests/test_purchase_sync.py (SCM440 재질 숫자를 지름으로 오인·형상 미구분 사고 방지)
- **판매가 = products.sale_price(표준·계약가, 마스터 관리 제품 상세 입력) →
  최근 출고가** 순 — 평균가는 판매가·마진 계산에서 제외 (Migration 050/051,
  2026-08-31 사용자 확정: 단가는 특정 시기에 바뀌므로 변동 내역이 중요하고
  평균은 무의미). 실적 참고치는 recent_price / avg_unit_price_12m 로 별도 유지.
  **sale_price 변경은 master_change_log 에 자동 기록**(변경 사유 입력란 포함,
  제품 상세 저장 시) — 이력은 원가 확인 > 제품 원가 상세에 표시.
  마스터 관리 제품 리스트·상세에는 **대표 단가(마스터 > 최근 출고가)를 항상 표시**하고
  근거를 메모로 붙인다(2026-09-01). 최근 출고가는 표시용 — 값을 바꾸거나 "확정" 체크한
  경우에만 마스터로 저장(무단 마스터화 금지)
- **수주 = 진행 의사 (2026-09-01 사용자 확정)**: 미납 수주가 미등록·휴면 제품에
  걸리면 수주 관리 상단 "제품 확인 필요" 패널에서 조치 — 경로는 **두 가지뿐**: 휴면은
  단가·소재비 변동을 보여준 뒤 활성화(단가 갱신 시 master_change_log), 미등록은 수주
  정보만으로 최소 등록(BOM·라우팅은 BOM 편집에서; 등록 시 customer_part_mapping 저장 →
  업로드 매칭이 우선 참조). 그 외 재매핑 UI는 만들지 않는다(사용자 확정: 매칭 로직이
  맞으면 생기지 않는 이레귤러). **업로드 매칭 규칙**: 정확 품번 일치는 휴면 포함 허용
  (4S20ABV-FL-01 처럼 재질 계열 별도 품번이 휴면으로 존재), 4S/S 접두어를 뗀 후보는
  활성 제품에만 허용 — 휴면에만 닿으면 미매칭으로 두고 패널에서 신규 등록.
  **예외: archive_reason 에 '통합' 이 있는 휴면 품번(접두사 제거 정책으로 합쳐진
  옛 품번)은 별칭일 뿐** — 정확 일치라도 접두어를 뗀 활성 본품을 우선하고, 활성이
  없을 때만 그 휴면으로 (2026-09-10: '4S20AHYBV-03-X1413;PM' 별칭이 휴면 P0010 에
  걸려 새 수주가 휴면 제품에 붙던 문제). 확실한 매핑은 customer_part_mapping 에
- **단가 변동 감지 = 수주 대체 / 별건 유지** (2026-09-10 사용자 확정, Migration 059,
  utils/so_replace.py): 같은 거래처·품번의 미납 라인 중 단가가 다른 옛 라인들을
  **품번당 카드 하나**(새 = 수주일이 가장 늦은 라인, 옛 = 나머지 전부, 체크로 포함
  선택)로 수주 관리 상단 패널에 띄운다 — 옛 수주가 여러 건으로 나뉜 채 한 번에
  재발주되는 실제 케이스(2026-09-10 20/40/80/150AHYBV) 대응. **도구는 판단하지
  않는다** — 추천만(새 수량 ≥ 옛 미납 합계의 95% → 대체, 뚜렷이 작으면 별건)
  표시하고 사람이 [대체] / [별건 유지]. 특수 단가(price_kind) 옛 라인은 후보 제외.
  대체: 체크한 옛 라인 모두 pending 0 · status REPLACED · replaced_by_soi,
  옛 회차는 qty=delivered 로 닫고 남은 잔량을 새 라인 회차로 이관(새 라인에 회차가
  없을 때만), 옛 수주 헤더는 전 라인 미납 0 이면 CANCELLED(+remark), 체크 시 마스터
  sale_price 갱신 + master_change_log. 별건: 새 라인 price_kind=PROJECT, 마스터 유지
  (프로젝트 단가는 라인에만 — 표준 마진은 마스터, 실제 마진은 출고 라인 단가).
  결정은 `so_line_replacements`(닫은 회차·이관 회차·마스터 이전값 보관) 에 남아
  다시 묻지 않고, 이력 expander 의 [되돌리기]로 원복. 라인 status 'REPLACED' 는
  chk_soi_status 에 추가됨(STATUS_KO '대체됨'). **UI 는 최소** (2026-09-10 사용자
  피드백 "부연설명 과다"): [대체] 는 옵션 없이 항상 마스터 단가 갱신 + 남은 회차
  이관, [별건] 은 새 수주만 특수 단가로 표시하고 종류(프로젝트 / 사급 소재
  CUSTOMER_MAT, Migration 060)만 기록. 옛 수주가 특수 단가인 예외(20AHYBV 사급
  4,100)는 라인 price_kind 를 직접 지정(DB) — 특수 단가 옛 라인은 후보에서 제외되어
  반복해 뜨지 않는다. `tagged_soi_id`/`tagged_kind` 로 되돌리기가 표시한 라인만
  지운다. 마스터 sale_price 가 NULL 이면 카드에 '미설정' — 대표 단가(최근 출고가)와
  혼동 주의
- 이미지 발주서(AI 비전) 파싱과 Anthropic API 키는 **매입 전표 기능 때 함께** 도입 —
  그 전까지 이미지 발주서는 수기 입력 (2026-09-01 사용자 확정)
- 실사 재고는 일괄이 아니라 **품목별로 확인되는 시점에 ADJUSTMENT 로 기점 설정**,
  라우팅도 진행하며 제품별로 등록 (2026-09-01 사용자 방침). **실사 방식 3종**
  (2026-09-07 확정, 마스터 관리 > 완성 재고 맞추기): ① LOT별 실사 — 라벨·이동표로
  LOT 을 아는 실물은 LOT 별 ADJUSTMENT ② 기초 재고 LOT(OB) — LOT 을 알 수 없는
  이전 재고는 `OB-YYYYMMDD` LOT 으로 등록(조정일이 완성일 → 새 LOT 보다 먼저 출고)
  ③ 제품 합계 조정 — LOT 없이 총량만(증가분은 OB LOT, 감소는 LOT 없음). 8월
  "재고 없이 출고" 로 제품 합계가 음수인 품목은 LOT 잔량은 정상이므로 ①②로
  실물을 넣으면 합계도 맞는다. **정리 방침 확정 (2026-09-11)**: 공정 입력 지연으로
  출고 > 완성인 상태는 별도 정리 기능·예외 규칙 없이 **품목별 기점 실사**로만
  닫는다 — 기준일(date_input)·LOT·실물 수량·소재 LOT(W번호, 히트 추적)을 OB 등록에
  넣으면 ADJUSTMENT.work_order 에 W번호가 남고 product_lot_stock_v 가 배치 없는
  LOT 에도 material_lot 로 보여준다(Migration 061). 슬랙 #외주관리 검사 글은 정리
  때 참고 자료(검토표 https://claude.ai/code/artifact/dff316e2-3c60-464b-a5fd-3deb701fa328)
  로만 쓰고 앱으로 가져오지 않는다. 장기 방향은 **앱 → 슬랙 푸시**(검사 판정·출고
  확정·투입·발주·주간 요약을 채널로 게시), 슬랙 → 앱 입력은 하지 않는다
- **생산 계획 기초 = 공정 표준** (2026-09-07 사용자 확정, Migration 055): 계획
  단위는 **작업 = 품번 × 공정 스텝**(CNC#10 …), 작업은 장비군(CNC/MCT)에 배정,
  품목 완성 = 마지막 작업. 품목 단위로는 공정별 수량·시간이 달라 부하를 셀 수 없다.
  `product_op_std`(표준 UPH·준비시간·장비군·상태 AUTO/CONFIRMED), `product_op_machine`
  (품번×공정×설비 실적 = 능력 매트릭스), `machines`(장비군·주야 2교대·교대당 9h·가동).
  표준은 생산 보고 MES 실적에서 자동 산출(utils/op_std.py — 구간별 UPH 중앙값, 야간
  자정 넘김 처리, 설비명 정규화 'MCT 03'→'MCT03') → 생산 계획 > 공정 표준에서
  확인·[표준 확정]. **확정 표준은 재산출해도 덮어쓰지 않는다**(자동값만 갱신 —
  표준이 흔들리면 전개가 힘들다는 사용자 경험). 가동률 = 표준 UPH 대비 생산량.
  **공정 표준 탭 = 품번 리스트 → 그 품번의 작업(공정 스텝) 편집 표** (2026-09-07
  개편, Migration 058): 표준은 공정 설계 단계의 작업 표준(공정 개선 때만 변경)이라
  data_editor(st.form + 편집 취소 nonce)로 직접 고치고, 저장 행은 CONFIRMED 로 잠김.
  시트 UPH 목표·실적 중앙값은 읽기 전용 참고 컬럼. `active`(사용) 끄면 작업 큐·
  스케줄에서 제외(시트 변형 공정 정리용). 변경은 `product_op_std_log`(항목·이전·
  이후·사유). 시트 품번이 제품 미연결이면 상세에서 검색해 연결(그 품번 전체 작업에
  적용), 새 품번은 expander 에서 스텝 콤마 입력으로 등록(MANUAL·CONFIRMED)
- **생산 계획 첫 탭 = 제품별 자재 필요량** (2026-09-07 사용자 판단: 자재별보다
  업무 지표로 효율적). 제품 한 줄 = 미납·완성 재고 충당·순생산필요·가장 이른 납기·
  소재 필요량·소재 부족(소재 재고를 납기순으로 선착 배분) → 상세에 BOM 소재별
  필요/재고/배분/부족 + 미납 수주 라인. 자재별 탭은 "자재별 필요량 (구매)" 로 유지
  **실적 원본 = 구글시트 "26년 생산일정"** (Migration 056, utils/prod_sheet.py):
  [데이터DB] 탭(날짜·교대·장비·공정명 '품번#스텝'·C.T.·생산량·가동시간·불량·UPH목표·
  가동/비가동·정지사유, 2026-06-02~) → production_log(source SHEET_DB, sheet_key 로
  중복 방지, uptime_hours 직접), [제품별 목표생산량] 탭 → product_op_std.uph_sheet/
  ct_sec(std_source SHEET). **표준 우선순위: 확정값 > 시트 UPH 목표 > 실적 중앙값.**
  동기화는 생산 보고 > MES 업로드 탭의 "생산일정 시트 동기화"(xlsx 업로드 또는 공개
  링크 다운로드 → ① 확인 → ② 반영). 시트 품번은 '-VM' 을 뺀 약칭 → pn_key 매칭
  (alias_list 참조), 설비명 괄호 설명 제거('CNC13(다인로봇)'→CNC13). 공정명은 시트
  표기 그대로('MRG6-07#20#30' 처럼 합병 공정 = 공정 자체가 바뀐 것, 사용자 확인).
  7/2~7/3 MES 시험 업로드 297행은 시트와 중복이라 삭제됨. 기존 MES 업로드 경로는
  유지(공정명 'CNC#10' 체계라 시트 체계와 다름 — 혼용 금지)
  다음 단계: 회차 스케줄 → 작업별 순생산필요 → 장비군 주간 부하 → 작업지시 장비
  배정·간트 → MES 대사(앱 검증 후)
- **설비 스케줄 보드 1단계** (2026-09-07 목업 승인 후 구현, Migration 057,
  utils/schedule.py, tests/test_schedule.py): 생산 계획 > 설비 스케줄. **셀 = 설비 ×
  날짜 × 교대(주간/야간)**, 셀 하나에 작업(품번#스텝) 하나 — `production_schedule`
  (unique machine_id·plan_date·shift). 왼쪽 **작업 큐** = 미납 품번의 작업 목록
  (잔여 = 미납 − 완성 재고 − 열린 배치 재공, 필요h = 잔여 ÷ 표준 UPH, 납기 = 가장
  이른 미충당 회차, 배정 = 이번 주 이후 계획 합, 가능 설비 = 능력 매트릭스) →
  → expander 로 접힘. **보드가 메인** (2026-09-07 사용자 피드백 "내용이 많아
  쓰기 어렵다"). **보드 = 자체 커스텀 컴포넌트** `components/schedule_board/index.html`
  (2026-09-09 현장 요청 "캘린더에서 직접 넣고 빼고 한 번에", PC 마우스 기준):
  Streamlit 컴포넌트 프로토콜을 라이브러리 없이 직접 구현(componentReady / render
  / setComponentValue / setFrameHeight), `declare_component(path=…)` 정적 서빙이라
  Cloud 에서도 빌드 없이 동작. 조작: 빈 칸 클릭 → 검색 팝오버(타이핑 필터, ↑↓ Enter)
  → 배치, 칩 드래그 이동(Ctrl = 복사), 칩 모서리 채우기 핸들로 같은 행 연속 칸 채움,
  × 빼기, Ctrl+Z 되돌리기, [변경 취소], [저장] 은 변경 목록만 `{seq, nonce, changes}`
  로 앱에 전달 → 앱이 upsert/delete(수량은 서버가 표준 UPH × 교대 시간 − 준비시간으로
  재계산) → `sb_comp_gen` 증가로 nonce 를 바꿔 컴포넌트를 DB 상태로 재초기화, 성공
  메시지는 `sb_flash` 로 rerun 뒤 표시. (nonce, seq) 로 같은 저장을 두 번 적용하지
  않는다. 야간 미운영 설비 칸은 드롭·배치 거부. 실적 색 칩(계획→진행→달성/미달,
  비가동 정지사유, 계획 없는 실적 점선)은 컴포넌트가 그린다. 컴포넌트 파일이 없으면
  data_editor 격자/HTML 보드 폴백. 브라우저 자동화 도구의 키 입력은 iframe 에 안
  들어가므로 검증은 같은 출처 JS(contentDocument)로 한다. 장비군 필터로 행 축소.
  기간 일괄 입력·수량/메모/작업지시 조정(비움 체크)은 expander.
  부하 = 계획 시간 ÷ (가동 설비 × 교대 × 교대당 시간). 다음 단계: 계획 대비 실적
  주간 요약 이관 → 선행 공정 재공 검증·준비시간 자동 가산 → 납기 시뮬레이션·자동
  배정 제안 → MES 연동. **utils 모듈을 고치면 로컬 dev 서버는 재시작**(Streamlit 이
  utils 를 리로드하지 않아 옛 바이트코드로 실행됨 — 트레이스백 줄번호만 새 파일)
- **매입내역 운영 방식 (2026-08-31 사용자 확정)**: 당분간 구글시트("YYYY년_매입내역",
  월별 탭, 날짜는 같은 날 연속 행 첫 행에만 기재)가 원본 — 원가 확인 페이지의
  "매입내역 동기화"(① 시트 확인 → ② 실행, 자동 overwrite 없음)로 DB 반영.
  장기적으로 발주 입고 시 매입 전표 자동 생성 + 전표/스캔 입력으로 전환 예정
  (전제: 발주서 양식 안정화 먼저). 시트 ID 는 app_settings 'purchase_sheet_id'
  (없으면 utils/purchase_sync.DEFAULT_SHEET_ID), 공개 링크(뷰어) 공유 필요
- **제품 사이즈 = products.product_size** (완성품 치수, 발주서 품명 괄호 "4PDVN-03 (Ø40*23.5L)")
  — Migration 052 (2026-09-02 사용자 확정). 자재(소재) 치수는 BOM 자재의 spec 이 진실.
  `products.raw_material_spec` 은 옛 소재 규격 스냅샷으로 **deprecated — UI·프리필에 쓰지 않는다**.
  발주 라인 프리필: 품명 = "품번 (제품 사이즈)", 규격 = BOM 자재명
- **확정 출고 전표의 정정 = 같은 전표번호 유지 + 차이(delta)만 역반영** (2026-09-04
  사용자 확정, Migration 053/054). 확정은 실물 출고 시점 그대로(재고·수주의 진실),
  정정은 출고 관리 > 출고 전표 > 확정 전표의 [전표 정정] — 라인 수량 변경(0 = 라인
  제외), 사유 필수, confirm_gate. 감소분은 그 라인이 차감한 LOT 를 마지막 LOT 부터
  **양수 ISSUE 행**으로 복원(원장 append-only, `issued_lots` 는 LOT 별 net), 회차
  납품완료는 `shipment_allocations`(확정 때 라인→회차 충당 저장) 역순으로 되돌림
  (저장 없는 옛 전표는 지정 회차 → 최근 납품 회차 폴백), 수주 라인 received/pending/
  status 재계산. 증가분은 확정과 같은 규칙(미납 한도·재고 검사·회차 충당·FIFO).
  이력은 `shipment_revisions`(이전/이후 수량·사유·담당), 헤더 `rev_no`/`revised_at`,
  재발행 문서에 '정정본 vN · 날짜' 표기(옛 출력물 파기 안내). **[전표 취소]** 는 전
  라인 0 역반영 + CANCELLED(라인 수량은 기록 보존, 사유 필수). 순수 계산은
  utils/shipment_adjust.py(tests/test_shipment_adjust.py), 흐름은
  tests/test_shipment_revision_flow.py. 거래명세서 수령 확인 단계는 규모가 커지면
  추가(현재는 부가 업무라 보류 — 사용자 결정)
- **월 마감 잠금 = `sales_month_close`** (영업 보고 > 월 마감, 관리자만 잠금/해제,
  confirm_gate). 마감된 달의 출고일 전표는 확정·정정·취소가 차단된다 — 세금계산서
  발행 후 수량 변경으로 매출 집계가 어긋나는 것을 막는 장치. 정합 점검
  `SHIP_ALLOC_MISMATCH`(2026-09-05 이후 확정분의 라인 수량 vs 회차 충당 합)
- 상태값은 코드화: active '1'/'0', 조달 '도급'/'사급'

## DB 작업 규칙

- 스키마 변경은 Supabase MCP `apply_migration`으로 (이력 유지). 2026-09-11 기준 마이그레이션 061까지
- **뷰 재정의는 항상 전체 정의를 명시** — `create or replace view v as select * from v ...`
  같은 자기 참조는 DDL 은 통과하지만 조회 시 무한 재귀로 뷰가 죽는다 (046 사고, 047 복구).
  기존 검사에 추가할 때도 전체 UNION 을 다시 쓴다
- **CHECK 제약과 뷰를 함께 확인**: 상태값을 새로 쓰기 전에 해당 컬럼의 CHECK 제약에 값이 있는지
  확인할 것 — 041에서 뷰만 CLOSED를 알고 제약이 거부해 종결이 조용히 실패했던 사례(042에서 수정).
  `db.update`는 실패 시 False만 반환하므로 앱에서는 else 분기로 오류를 반드시 표출
- **새 테이블은 반드시 `ENABLE ROW LEVEL SECURITY`**, **새 뷰는 `WITH (security_invoker = true)`** — 정책은 만들지 않음 (anon 차단이 목적, 앱은 service_role)
- 파괴적 변경 전 백업 테이블(`*_backup_MMDD`) 생성 관례 유지
